# 珂莱塔数字人导游：开源参考与并行开发提示词包

以下保存用户提供的第 0—2 节正文。用户另明确：形象使用 kelaita；许可证不同时可以自行解决适配问题。本轮仅 M0，READY 后交接 A/B/C/D，M1 另行执行。

## 0. 定位与使用顺序

项目是“珂莱塔数字人校园导游 AI 应用”。desktop-pet 只是第一版人物形象的素材来源，不是应用形态，不是指定技术框架，也不意味着要复用桌宠软件。

项目目录：E:\AI4TJU。形象来源：C:\Users\ASUS\Desktop\desktop-pet，只读。

本轮交付目标：参考并实际复用合适的开源实现，完成精美网页、珂莱塔形象展示、指定 GLM 真实对话、运行态调用日志，并接入基础校园知识；语音和场景采用可替换接口逐步完善。现有形象能力有限时如实记录，不能把图片说成可捏脸的三维模型。

本文是本地 Codex 的开发任务包，不代表这里已读取 C/E 盘、已生成项目或已验证 API。

五个窗口：

|窗口|职责|什么时候开始|
|---|---|---|
|M 总控|开源选型、项目初始化、统一契约、合并、最终验收|先执行 M0，最后继续 M1|
|A 前端与场景交互|精美工作台、对话、来源和日志展示、场景交互入口|M0 输出 READY 后|
|B 数字人及语音|借用形象适配、人物状态、ASR/TTS 适配与打断|与 A/C/D 并行|
|C 模型与编排|指定 GLM、任务流程、真实调用日志、错误处理|与 A/B/D 并行|
|D 校园数据|小规模真实知识、来源核验、建筑索引与检索|与 A/B/C 并行|

顺序：M0 选型与冻结接口 → A/B/C/D 同时开发 → 同一窗口 M 执行 M1 汇总验收。

所有项目代码、素材副本、文档、工作树都在 E:\AI4TJU 下。建议主目录做集成工作树，四个工作树位于 .worktrees\ui、.worktrees\avatar、.worktrees\api、.worktrees\knowledge。实际路径与分支由 M0 检查后生成；遇到重名不得覆盖。

先把本文保存到 E:\AI4TJU\AI4TJU_数字人导游_开源参考与并行开发提示词包.md，再按各节复制提示词。文件末尾有每个窗口的一句话启动指令。

## 1. 总目标、开源复用原则与阶段边界

### 1.1 用户最终需要的产品

面向天大访客和新生的数字人导游，有自身形象和统一视觉设计。

能连接指定大模型进行知识问答、内容生成、多轮对话。

有真实运行态日志，能说明请求、检索、工具、语音和人物动作发生了什么。

支持形象定制，逐步加入真实语音、动作表情、场景理解及三维校园联动。

初期借用现有形象，以后替换人物资产时不重写对话和业务。

### 1.2 当前先跑通的范围

确定开源复用路线，能说明具体采用了哪些组件/代码、参考了哪些设计。

将用户现有角色作为第一版珂莱塔展示，建立统一 AvatarAdapter。

漂亮、可使用的数字人工作台；不能只是宣传首页或把开源项目换标题。

指定 GLM 的真实聊天与多轮历史，明确错误、重试、取消和运行日志。

少量有来源的校园知识与点位信息，证明检索和场景对象可以关联。

根据选中底座和可用资源接通一条可用语音链路；缺服务时明确未完成，同时保留文字主链路。

保留真实能力驱动的形象设置和场景入口；三维场景接口可预留，但未实现功能不得摆放成“可用”的假按钮。

完整三维校园、任意地图自动建模、重新训练数字人、自由捏脸与照片级口型不要求在这一轮完成。它们保留为后续目标。背景/缩放是展示设置，不算完成捏脸；没有可变形模型时不得把该项判为通过。

### 1.3 必须评估的开源参考

成熟度须根据本轮实际版本判断，不把活跃或星数高直接等同于生产稳定。

|项目|重点评估/复用内容|已知需要核验的边界|
|---|---|---|
|https://github.com/Open-LLM-VTuber/Open-LLM-VTuber|完整语音对话、打断、人物状态、模型提供方适配、会话|Live2D 是原有主形态；主代码与素材/SDK 授权分开；关注版本重写状态|
|https://github.com/moeru-ai/airi|数字人舞台、人物接口、VRM/Live2D、语音与状态组织、前端体验|仓库复杂度与运行方式；保留其实际框架，不先强制迁移|
|https://github.com/met4citizen/TalkingHead|网页 3D 数字人、表情、动画、音频和口型同步接口|骨骼与 Blend Shapes 要求；中文口型不能照搬英文演示结论|
|https://github.com/pixiv/three-vrm|VRM 运行层、表情和人物控制|只在确有 VRM 路线时采用，不自带完整对话/捏脸|
|https://github.com/langchain-ai/langgraph|有状态工作流、检索和工具编排、取消与追踪的组织方式|不保证指定网关有原生 tools；固定工作流与自主工具调用分别记录|
|https://github.com/fastapi/full-stack-fastapi-template|独立前后端工程布局、开发与部署实践|仅组件式路线需要；避免把账号、数据库等无关功能全部引入|

复用强度要明确标注：直接依赖、保留许可的源码复用、仅设计参考、未采用。只有阅读 README 或列出项目链接，不算实际代码复用。

选择一个主路线：

完整应用二次开发： 若 Open-LLM-VTuber/AIRI 的实际版本易运行且符合展示与语音需求，优先沿用其结构、依赖与现有链路，新增珂莱塔业务。

成熟组件组合： 若完整底座带来的约束过大，采用独立工作台，直接使用合适的人物运行库和成熟后端/编排组件；用事实说明为何选择此路线及哪些模块确实复用。

不能把整个 AIRI、Open-LLM-VTuber、TalkingHead、Dify 同时拼到一个项目，也不能以“先最小化”为由跳过开源评估后把所有底层模块重新手写。

## 2. 所有窗口的共同约束

### 2.1 目录、Git 与权限

先阅读适用 AGENTS.md；保留用户现有文件、代码、分支和未提交修改。不重置或清空 E:\AI4TJU，不自动迁移先前其他项目。

C:\Users\ASUS\Desktop\desktop-pet 仅提供形象。只读查看素材和资源引用；不运行未知二进制、不修改源目录、不复制整个桌宠工程作为应用底座。

A/B/C/D 只能改自己的 Git 工作树和 M0 分配的文件；不在同一工作目录并行改动。

仅 M 修改共享契约、依赖清单、锁文件、应用入口装配、根启动脚本和合并结果。其他窗口提依赖/契约变更建议，不能各自修改锁文件。

每个工作树使用独立依赖目录、虚拟环境、日志和端口；可复用正常下载缓存，不能通过可写目录链接共享 node_modules/venv。

.worktrees/、.env、.runtime/、缓存、虚拟环境和构建输出必须忽略；.env.example 保留。开发扫描和构建也必须排除嵌套工作树。

不擅自修改全局 Git 身份、系统代理、PowerShell 全局执行策略；不杀死其他程序释放端口。

本轮本地运行，默认只监听 localhost/127.0.0.1。用户未要求云发布，不启动 Sites 或其他云部署流程。

遇到必须新增的依赖或共享接口阻塞，工作窗口立即向 M 提交“阻塞请求”，不必等最终 handoff；同时继续独立部分。M 将必要改动集中为单独协调提交并给出同步命令，相关窗口先提交自己的改动、暂停共享接口使用，再同步一次并记录协调提交号。不能各自改锁文件。若暂不协调，允许以 PARTIAL 交付，由 M1 安装和补验，不能把未运行功能标 DONE。

### 2.2 模型配置

```dotenv
CAMPUS_LLM_URL=http://111.32.22.35:32592/mgate/v1/chat/completions
CAMPUS_LLM_MODEL=glm-5.1
CAMPUS_LLM_API_KEY=
```

真实密钥只在主目录未跟踪 .env 或进程环境中。禁止前端持有、VITE_* 暴露、日志打印、写入报告/测试夹具/提交。工作树不会自动继承主目录配置：由 M/C 通过显式配置文件参数或 AI4TJU_ENV_FILE=E:\AI4TJU\.env 读取，A/B/D 无需读取模型密钥。

优先复用已有授权配置；新窗口没有凭据时，提供一次本机隐藏输入步骤，不要求用户再次把密钥发到聊天。不猜测不确定的密钥拼接方式，不尝试多个候选密钥。若使用兼容 SDK，其基础地址应为 http://111.32.22.35:32592/mgate/v1；若直接 HTTP POST，使用完整 URL。两种方式选一种，不能重复附加路径。

先验证普通非流式对话，再决定是否开启上游流式或 tools。不能因为模型名字就声称支持图片、音频、Embedding、原生工具调用。网关访问失败不能悄悄改成别的模型。

### 2.3 真实性与能力边界

测试替身只出现在隔离测试。正常页面不能用预设回答、伪日志或随机 Token 数冒充模型成功。

日志记录真实事件与必要元数据，不记录隐藏思维链，不默认保存完整提示词、录音和认证头。

“配置存在”“HTTP 服务在线”“模型真实调用成功”是三个不同状态。

动态图不是三维模型；摇晃不是准确口型；更换背景不是捏脸。不能为了验收而更改术语掩盖能力缺口。

语音分为 ASR、LLM、TTS 和人物播放，分别检查。优先使用选中开源底座已有的提供方适配；浏览器语音仅是可用时的替代/降级方案，不能宣称已接入另一套专业语音服务。

保留最终数字人应用目标，当前未做的功能写进后续清单，不从需求中悄悄删掉。

---

用户提供的正文到 2.3 截止；未提供后续各窗口完整提示词。M0 的具体执行要求沿用首次用户消息。窗口启动指令由 M0 生成在 docs/PARALLEL_RUN.md。
