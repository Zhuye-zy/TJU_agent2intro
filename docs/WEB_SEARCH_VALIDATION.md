# 联网与地图增量验收

日期：2026-09-14。分支：`feature/web-search-map-location`。

## 已完成

- 完整前端构建及适配器构建通过。
- 后端隔离回归：76通过；前端/地图/语音/传输隔离回归：59通过。
- 真实联网搜索：返回校史馆相关结果，并读取天津大学 MPA 网站的官方页面。检索摘要与已读取正文有明确区分。
- 真实讲解词生成：`glm-5.1`，正常完成，219字符，约29秒，最终含1条联网来源。
- 真实社交文案：正常完成，185字符，约35秒。此请求搜索不可用，使用已有资料继续完成。
- 真实参观计划：正常完成，202字符，约42秒。此请求搜索不可用，使用已有资料继续完成。
- 高德 JS SDK 下载 HTTP 200；带 JS 平台参数的 IP 请求返回业务 `OK/10000`。
- 经应用本地安全代理再次请求 IP 城市接口，返回成功且包含城市与范围。未将 IP 或坐标写入验收报告。
- 语音清单恰好4项；原女声/男声普通话/粤语/英语均真实合成有效音频，字节数分别为14400、15696、17712、16272。
- 跟踪候选文件及构建273个文件，未发现本机已配置的模型/高德密钥值。`.env` 与 `.runtime` 仍被忽略。
- 本机应用已通过项目管理脚本启动，地址 http://127.0.0.1:8000 。

## 验收边界

当前会话没有可控制的浏览器，因此没有把接口验收写成地图已经实际渲染、持续定位已现场运行或扬声器已听音。持续轮询、取消、后台暂停由代码实现；IP范围解析、取消和不能用粗略位置规划精确路线已有隔离测试。

公开搜索服务可能限流或超时；本次三次并发生成中两次触发搜索降级，均保留了正常回答。引用提示不意味着逐句事实已经核验。现有校园点位仍缺少经过核验的导航坐标，在线底图和IP定位不会自动补齐入口/门禁信息或藏品数据库。

## 本机验证步骤

1. 打开或刷新首页；在线地图应自动加载。
2. 在内容生成中输入校史馆介绍，生成讲解词；展开参考资料和运行日志观察联网结果。
3. 点击“IP 城市定位”或“持续 IP 定位”。持续更新间隔30秒，可随时停止；切校区后停止。IP表示出口网络所属粗略区域。
4. 开启语音，切换四个选项并实际播放。若浏览器拦截自动播放，点击“恢复声音”。
