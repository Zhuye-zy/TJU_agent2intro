# R2 D 坐标与通行核验

本批 92 个 POI 的 `location`、`entrances` 和 `schematic_position` 全部为 `null`/空数组；已核验经纬度为 0。没有高德 Key、获准导出或经过地理配准的独立坐标资料，故未做 WGS84/GCJ02 转换，也没有用官方平面图像素、校区中心或随机偏移补点。

双校区各 10 点地图核验和 3 条代表路线均未执行，状态 NOT_TESTED。高德返回结果、若未来有授权，只能作为 `ProviderCrosswalk` 维护，需记录原始 CRS、来源、匹配日、入口/建筑中心/近似质量及厂商缓存条款；不能成为校园通行已核验的证据。
