# R2 D 数据覆盖审计

数据版本：`sha256:903be5057aa1`（运行时对受管 JSON 计算；变更后会不同）。获取日期均为 2026-09-14。

| 层 | 实际数量 | 说明 |
| --- | ---: | --- |
| 发现页面 | 6 | `SOURCE_REGISTRY.json` 的已读取来源；另有 robots URL 响应错误，未把它解释为允许或禁止。 |
| 成功读取 | 5 直接正文 + 1 搜索索引正文 | 北洋园导览直接打开返回错误，只使用检索器已展示的名称目录。 |
| 正文提取 | 5 | 只整理短事实，不存网页全文、图片或地图。 |
| 去重来源 | 6 | canonical URL 为主键；未保存全文 hash，故 hash 为 null。 |
| 旧摘要文档 | 7 | 第一轮遗留；不是原子事实总数。 |
| POI 目录事实 | 92 | 每项是“官方页面列出该名称”的独立、可回查断言。 |
| 原子事实 | 99 | 7 旧摘要 + 92 POI 目录断言；未把同一页面复制成多 chunk。 |
| chunk | 0 | 没有保存页面全文，故不虚报 chunk。 |
| POI | 92 | 卫津路 45、北洋园 47；同一北洋园东门复用既有 ID。 |
| 有核验经纬度 / 入口 | 0 / 0 | 不把导览列表、示意图或校园中心点转换成经纬度。 |
| 可显示实景媒体 | 0 | 许可未确认，见 media manifest。 |

POI 目标（80、每校区30）已经超过；200 个不同原子事实目标仍差 101，状态为 **PARTIAL**。8 类中卫津路有全部类别，北洋园缺 library 的 R2 POI 投影（既有郑东图书馆仍在 legacy building 视图）；下一轮应在有明确正文出处时将其迁入同一 POI ID，不以名称猜补。

检索为本地关键词、别名和中文二元 n-gram，不是向量检索。目录使用独立 `limit`（1–100）和版本、校区、类别、查询绑定的 cursor；聊天检索保持原端点 `limit`（1–20）。查询和服务导入不访问网站。

维护命令：`python -m backend.knowledge.importer` 仅审计；`--staging <已人工核验目录> --dry-run` 预检；不带 dry-run 原子替换并生成 `.backup-last`；`--restore-last-backup` 恢复。该命令不抓取网站；抓取失败、限速和候选链接均应先记录到来源登记，再人工审核进入 staging。
