# D 知识数据目录

`documents.json` 是短事实摘要，`buildings.json` 是可供场景关联的建筑对象，`SOURCES.md` 记录来源与素材使用条件。运行时从这些文件计算数据版本和实际条数；不会联网、嵌入或下载素材。

当前检索是关键词、别名与 CJK 字符 n-gram 匹配，不是向量检索。只加入核验过的真实资料与来源元数据；无证据的发布日期或坐标填 `null`，不填假数据。遵守 `docs/CONTRACTS.md`。
